const user = {name:"rong", age: 10};

// simple type
let counter: number;
counter = 1;

// non-primitive type
let arrayName: string[];
let person: {
    name:string;
    age:number;
}
person = {
    name: "John",
    age: 25
}